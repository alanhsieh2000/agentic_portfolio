# Build the interactive orchestration flow


This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds. This plan must be maintained in accordance with `PLANS.md` at the repository root. This plan builds on all five prior plans, each checked into this repository: `plans/01_dataset.md` (factor and price data), `plans/02_llm_s_agent.md` and `plans/03_llm_f_agent.md` (the two screening agents), `plans/04_candidate_scanner.md` (consensus logic), and `plans/05_optimizer_and_allocation.md` (weights and share allocation). This plan is the last piece: it runs all of the above together and lets a person steer the result interactively.


## Purpose / Big Picture


After this plan is done, a person can run one command and step through the whole pipeline this project builds, end to end, for a date of their choosing: see which stocks LLM-S and LLM-F each flagged, see the consensus candidate list the scanner produced from those two signals, see the resulting portfolio weights and share allocation under whichever of GMV, MV, or MSR they pick, and then — this is the part `README.md` specifically calls for — add or remove tickers from that candidate list by typing a request, and immediately see the weights and allocation recomputed for the edited list. The same command works two ways: pointed at a historical rebalance date within 2020-01-01 to 2024-04-30 (backtest mode, letting a person watch the paper's whole evaluation window play out and compare against the paper's own reported Sharpe ratios), or pointed at today (live mode, producing an actual current portfolio recommendation using live data).


## Progress


- [x] (2026-09-04) Confirmed the installed CrewAI version's current pattern for building an interactive, multi-turn flow, per this plan's Concrete Steps Step 1 and `CREWAI.md`'s mandatory freshness check. Installed version is `crewai==1.15.12`; the latest on PyPI at the time of this check is `1.15.18` — six patch releases ahead, with no breaking `Flow`/human-in-the-loop changes in between per `https://docs.crewai.com/en/changelog` (the notable HITL-adjacent entries are conversational flows promoted to stable and a raised Anthropic default `max_tokens`), so the gap was reported to the repository owner rather than silently upgraded. A stable, documented `@human_feedback` decorator does exist in this installed version (`crewai.flow.human_feedback`, exported from `crewai.flow`, documented at `https://docs.crewai.com/en/learn/human-feedback-in-flows` as requiring v1.8.0+, not marked experimental) and was verified empirically to work here, including a self-looping multi-turn edit loop via `@listen(or_(...))` plus `@router`, and requiring no LLM or API key at all when `emit` is omitted — see Surprises & Discoveries for the probe and its transcript. The decision recorded in the Decision Log is nevertheless to use a plain Python `input()`-based loop calling this plan's own `edit_candidates` directly, because CrewAI's own default feedback provider is literally `input("Your feedback: ")`, free-text feedback arrives unparsed (`outcome=None`) unless `emit` is used, and `emit`'s outcome routing requires an LLM (defaulting to OpenAI's `gpt-5.4-mini`) to classify inputs as simple as `add NVDA` in an otherwise Anthropic-only project. This item is research and a decision only — `src/flow/` itself remains unimplemented, and every other item below is still pending.
- [x] (2026-09-04) Implemented backtest mode: given a rebalance date in the stored 2020-2024 window, `run_pipeline` (new `src/flow/interactive.py`) runs LLM-S and/or LLM-F, the scanner, and the optimizer in sequence and returns every intermediate result; a new `src/flow/cli.py` (`main()`, `uv run python -m src.flow.cli --date ... --objective ... --value ...`) displays it — the rule (if run), the scanner's branch and candidate list, the weights, and the share allocation — closing this item's "display the results" half directly rather than deferring it to the still-pending editing-loop item below. Built the `selection` parameter (next item) into `run_pipeline` from the start, per the Decision Log entry recorded when `plans/08_consistency_review.md` findings 9/10 were resolved, which asked for exactly this so the function would not need a second pass. Live mode (a `rebalance_date` outside the stored window) and the interactive add/remove/finish loop are deliberately out of scope for this item — `run_pipeline` raises `NotImplementedError` for the former (see the item below) and `cli.py` does not yet loop (see the candidate-editing item below).
- [x] (2026-09-04) Gave `run_pipeline` (and, by the same signature, the not-yet-implemented `run_full_backtest`) a `selection: str = "llm_s_only"` parameter (literal values `"llm_s_only"`, `"llm_f_only"`, `"llm_s_and_f"`) per README's Backtest Mode Stage 1 sentence "LLM-F only selection and LLM-S + LLM-F selection are all expensive evaluation. LLM-S only selection is the default selection." In `"llm_s_only"` mode, skips the LLM-F call (`screen_month`) entirely — not just ignores its result — and calls `scan_with_detail(llm_s_signals, None)`; in `"llm_f_only"` mode, skips LLM-S's `generate_rule`/`screen` and calls `scan_with_detail(None, llm_f_signals)`; in `"llm_s_and_f"` mode, calls both as originally specified. `src/scanner/candidate_scanner.py`'s `scan_with_detail` already had the three matching branches (`llm_s_only`/`llm_f_only`/`intersection`/`union`, confirmed correct in `plans/08_consistency_review.md` finding 12) — only `run_pipeline`'s call-site logic needed to conditionally build or omit each agent's call and signal argument. See `plans/08_consistency_review.md` finding 9 for the full rationale. Verified the skip is real, not just result-discarding, via a monkeypatched spy (`unittest.mock.patch` on `src.flow.interactive.screen_month`, with `generate_rule` also patched to a fixture `ScreeningRule` to avoid a real LLM call for this check): calling `run_pipeline(date(2024, 3, 1), "GMV", 100_000, selection="llm_s_only")` shows `generate_rule` called and `screen_month` never called (`screen_month called: False`), with `result["llm_f_signals"] is None` and `scan_detail["branch"] == "llm_s_only"`. A permanent monkeypatched-spy test covering all three `selection` values is still owed to the dedicated tests item below — this manual check only confirms the mechanism works, it is not the automated regression test.
- [ ] Implement live mode: same pipeline, but for the current date, using a freshly-fetched current S&P 500 membership snapshot rather than a stored historical one.
- [ ] Implement the interactive candidate-editing loop: let the user add/remove tickers from the scanner's output and re-run the optimizer and allocation on the edited list.
- [ ] Implement a simple month-over-month backtest runner that chains monthly rebalances across the full 2020-2024 window and reports the realized Sharpe ratio, for comparison against the paper's own S&P 500 baseline figure of 0.6324 over the same period.
- [ ] Make `run_full_backtest`'s return-and-Sharpe calculation use net, transaction-cost-adjusted returns and the risk-free rate, both now available as `settings.transaction_cost_bps` (default 10.0, i.e. 10 basis points) and `settings.risk_free_rate` (default 0.02) in `src/config/settings.py` (added while resolving `plans/08_consistency_review.md` findings 8 and 10 — see that plan's Decision Log for why these were added without the rest of plan 06). Per month `t`, with `w_t` the chosen weight vector and `w_{t-1}` the previous month's chosen weight vector (`w_0` = the zero vector, per README's Backtest Mode Stage 2 assumption $\hat{w}_{0,j}=0$), compute the turnover cost as `pypfopt.objective_functions.transaction_cost(w_t, w_prev, k=settings.transaction_cost_bps / 10000)` called directly as a plain numeric helper (confirmed in the installed `pypfopt==1.6.0` to evaluate to a float for `numpy.ndarray` inputs, since it is not required to run inside an `EfficientFrontier` optimization context) and subtract that cost from the month's gross weighted return to get the net return $r_t$. Compute the reported Sharpe ratio as `(r_t.mean() - settings.risk_free_rate / 12) / r_t.std() * (12 ** 0.5)` — a monthly risk-free rate obtained by dividing the annual `risk_free_rate` by 12, matching the monthly cadence of `r_t`. Two weight vectors being compared for turnover must be aligned to the same ticker universe first (union of both months' candidate tickers, missing entries treated as zero weight) since candidate sets can change month to month.
- [ ] Write `tests/test_interactive_flow.py` covering the candidate-editing logic, the selection-mode branching (asserting `run_pipeline` never calls the LLM-F signal function in `"llm_s_only"` mode, via a monkeypatched spy), and the net-return/Sharpe-ratio calculation against fixtures (a small fixture pair of monthly weight vectors and a hand-computable expected turnover cost and Sharpe ratio).
- [ ] Manually run both backtest mode and live mode end to end at least once each.


## Surprises & Discoveries


(Still to record, once the full-window backtest runner is implemented: the actual realized Sharpe ratio this project's simplified pipeline achieves compared to the paper's reported 0.6324 S&P 500 baseline and its own screened-portfolio results — a large gap either direction is worth understanding, given the fidelity gaps already recorded in plans 1, 3, and 5, namely survivorship-adjusted-but-otherwise-approximate factor data, thin historical news coverage, and PyPortfolioOpt's simpler covariance estimators in place of the paper's five custom ones. The CrewAI interactive-mechanism findings called for by Concrete Steps Step 1 are recorded below.)


- Discovery (2026-09-04): CrewAI's `@human_feedback` does *not* require an LLM or any API key so long as `emit` is omitted, and a repeated multi-turn edit loop really is expressible in it. Verified by running a throwaway four-method `Flow` against the installed `crewai==1.15.12` with piped stdin (`printf 'add NVDA\ndone\n'`): `@start()` produced a candidate list, a `@listen(or_("show", "again"))` method decorated with `@human_feedback(message="Edit candidates (add X / remove Y / done):")` displayed it and blocked for input, and a `@router` on that method returned `"again"` to loop or a differently-named outcome to exit. The transcript shows the loop body executing twice, once per piped line (`[probe] raw feedback='add NVDA' outcome=None` then `[probe] raw feedback='done' outcome=None`), followed by `[probe] finished` and `[probe] result: ok` with exit status 0, all with no `OPENAI_API_KEY` set and no LLM call made. Two mechanical gotchas found the hard way and worth knowing before anyone tries this again: a `@router`'s returned outcome string must not collide with the name of the handler that listens for it (`return "finish"` feeding `@listen("finish")` on a method named `finish` is rejected at class-construction time by a pydantic validator — `"A listener triggered by its own completion creates an infinite loop"`), and the feedback result is read off the flow instance as `self.last_human_feedback`, *not* `self.human_feedback` as both the live docs and the installed module's own docstring show (`AttributeError: 'ProbeFlow' object has no attribute 'human_feedback'. Did you mean: 'last_human_feedback'?`) — a real docs-versus-code drift in 1.15.12, and exactly the kind of thing `CREWAI.md`'s freshness-check requirement exists to catch.
- Discovery (2026-09-04): CrewAI's synchronous human-feedback path is not a richer input mechanism than a plain prompt — it *is* a plain prompt. `crewai/flow/async_feedback/providers.py`'s `ConsoleProvider.request_feedback` (the default provider when none is passed) collects the answer with a literal `feedback = input("Your feedback: ").strip()`, and `crewai/flow/runtime/__init__.py` has the same `input("Your feedback: ").strip()` call inline on its own path. Structured routing is the only thing the decorator adds on top, and that is gated behind `emit`, which `_validate_human_feedback_options` refuses without an LLM (`"llm is required when emit is specified"`); `HumanFeedbackConfig.llm` defaults to `"gpt-5.4-mini"`, an OpenAI model, whereas this project configures only `settings.llm_s_model = "anthropic/claude-sonnet-4-5"` and an `anthropic_api_key`. So the two available shapes are a bare `input()` with the feedback text handed back unparsed, or a bare `input()` plus a paid LLM round-trip to decide whether the user typed something meaning "add" or something meaning "done".
- Discovery (2026-09-04): the same probe run emitted 162 lines of terminal output for four flow methods and two feedback turns, almost all of it Rich-bordered `Flow Method Running` / `Flow Method Completed` / `Flow Completion` panels emitted by CrewAI's own event listener, with the four lines this plan's CLI would actually care about buried inside them. That is a real cost for a CLI whose stated purpose in Purpose / Big Picture is to *display* legibly why each ticker is or is not a candidate, alongside weights and a share allocation — suppressible, but only by additional work against CrewAI's event bus that a plain prompt loop never incurs.
- Discovery (2026-09-04): `settings.llm_s_model`'s default, `"anthropic/claude-sonnet-4-5"`, does not resolve against this environment's configured `ANTHROPIC_BASE_URL` (an Azure AI Foundry endpoint, `https://idofoundryrg.services.ai.azure.com/anthropic/`) — `run_pipeline`'s first real end-to-end run failed with `anthropic.NotFoundError: Error code: 404 - {'error': {'code': 'DeploymentNotFound', 'message': 'The API deployment claude-sonnet-4-5 does not exist. ...'}}`, the same Azure Foundry deployment-name model that `crewai.LLM` passes straight through to the Anthropic SDK. `plans/02_llm_s_agent.md`'s own manual-run item (2026-08-06) had already worked around this by overriding `LLM_S_MODEL=anthropic/claude-opus-4-8` for that one call, without ever updating `settings.py`'s default — this plan's own manual run hit the identical gap fresh, confirming it is a standing environment/settings mismatch rather than something plan 06 introduced. Re-ran with `LLM_S_MODEL=anthropic/claude-opus-4-8 uv run python -m src.flow.cli --date 2024-03-01 --objective MSR --value 100000` and it succeeded completely (real `generate_rule` LLM call, real `screen`, real `scan_with_detail`, real `compute_weights`/`allocate_shares`) — see Artifacts and Notes for the real output. Not fixed here (out of scope for this Progress item, which is about `run_pipeline`/`cli.py` existing and working, not about `settings.py`'s default model string) but flagged because every future real run of this CLI against the default model will hit the same 404 until either `settings.llm_s_model`'s default is corrected for this Azure Foundry deployment or `LLM_S_MODEL` is set in `.env`.


## Decision Log


- Decision: support both a historical backtest mode and a live/current-date mode, sharing the same underlying agent and optimizer code, differing only in which membership snapshot and which dates are fed in.
  Rationale: the user explicitly chose "backtest + live/current-date mode" over backtest-only when asked, wanting this project to be useful as an actual decision-support tool, not only a paper-replication exercise.
  Date/Author: 2026-08-05, decided by repository owner during planning interview.
- Decision: defer the choice of exact interactive mechanism (CrewAI `Flow` with `@human_feedback`, versus a plain Python `input()` loop calling this project's existing plan 2 through 5 functions directly) to this plan's own Concrete Steps, to be resolved by checking the installed CrewAI version's actual current support for human-in-the-loop interaction before committing to one approach.
  Rationale: `CREWAI.md`'s freshness-check requirement exists precisely because CrewAI's interactive/human-feedback features are recent (noted as "v1.8.0+" in that file) and evolving; committing to a specific mechanism in this plan without checking the installed version first risks writing a plan section that does not match reality.
  Date/Author: 2026-08-05, plan author.
- Decision: score each backtest month's realized portfolio return by reading `plans/01_dataset.md`'s shared `returns` table directly (the same table plan 5's optimizer reads for expected-return/covariance estimation), rather than this plan independently deriving "next month's return" from raw prices.
  Rationale: the user explicitly chose one shared returns table over letting this plan and plan 5 each compute "monthly return" independently, which risked the two disagreeing; this plan's backtest scorer is one of that table's two consumers, per `plans/01_dataset.md`'s Interfaces and Dependencies section.
  Date/Author: 2026-08-05, decided by repository owner during a plan-review follow-up question.
- Decision: when resolving `plans/08_consistency_review.md`'s findings 9 and 10 (this plan's `run_pipeline`/`run_full_backtest`, once built, needs a default-to-LLM-S-only selection mode and a transaction-cost/risk-free-rate-aware net-return and Sharpe formula, matching README's Backtest Mode Stage 1/Stage 2 text), specify the exact required signatures and formulas in this plan's Progress and Plan of Work sections now, add the two backing settings fields (`transaction_cost_bps`, `risk_free_rate`) to `src/config/settings.py` now, but do not implement `src/flow/` itself in that same session.
  Rationale: the repository owner, when asked how much of plan 06 to build while resolving those two findings, chose the "settings-only, defer plan 06 entirely" option, but asked that this plan's own pending-items list be expanded to capture the two new requirements precisely — so that whenever plan 06 is eventually implemented, it is built correctly the first time rather than needing a second consistency-review pass. Building the full interactive flow (live-mode scraping, the CLI, and a real 52-month backtest with real LLM calls per this plan's own Concrete Steps and Validation and Acceptance sections) was judged too large and too costly (real Anthropic API and network calls) to take on as a side effect of a documentation-consistency review.
  Date/Author: 2026-08-10, decided by repository owner during the plan 08 implementation session.
- Decision: implement `src/flow/cli.py`'s interactive editing loop as a plain Python `input()`-based loop calling this plan's own `edit_candidates` function directly, rather than as a CrewAI `Flow` with `@human_feedback`. This resolves the deferred choice recorded in the second Decision Log entry above and closes Concrete Steps Step 1. CrewAI is unaffected everywhere it already earns its place — `src/agents/llm_s_crew/` and `src/agents/llm_f_crew/` remain CrewAI `Crew`s, and this decision is only about the human-interaction layer above them.
  Rationale: five findings, each verified against the installed `crewai==1.15.12` rather than taken from documentation (details and transcripts in Surprises & Discoveries). First, `@human_feedback` genuinely works for this version and a multi-turn self-loop is expressible in it, so this is a choice between two working options, not a fallback forced by a missing feature. Second, CrewAI's default synchronous feedback provider is a literal `input("Your feedback: ")`, so the `Flow` route buys no better input mechanism than the plain loop. Third, the only thing it adds — collapsing free text into a routed outcome via `emit` — requires an LLM and defaults to OpenAI's `gpt-5.4-mini`, which would either introduce a second LLM provider into an Anthropic-only project or spend Anthropic tokens classifying strings as simple as `add NVDA`; per this plan's Plan of Work the editing step is deliberately simple set manipulation, so paying an LLM to parse it is backwards. Fourth, the `Flow` route prints ~40 lines of Rich event panels per method, actively working against a CLI whose whole point is legible display of candidates, weights, and allocation. Fifth, what the CLI actually needs is straight-line sequencing — prompt for a date, objective and portfolio value, call `run_pipeline`, print, then loop on edits re-running only `compute_weights` and `allocate_shares` — which is ordinary control flow with no branching, fan-out, or persistence that an orchestration engine would earn its keep on. Noted for a future contributor: if this CLI ever grows a non-terminal front end (a web UI, a Slack bot), `@human_feedback`'s `provider=` hook and `HumanFeedbackPending` async pattern are the right thing to revisit, since that is the case the decorator is genuinely built for.
  Date/Author: 2026-09-04, plan author, after the version and docs check required by `CREWAI.md` and by this plan's Concrete Steps Step 1.


## Outcomes & Retrospective


(To be filled in once this plan is implemented and validated, including the real multi-year backtest Sharpe ratio comparison called for in Surprises & Discoveries.)


## Context and Orientation


This plan adds `src/flow/interactive.py` (and a thin CLI entry point) to this Python 3.12, `uv`-managed repository. All commands below run from the repository root and assume all five prior plans are implemented: `data/portfolio.duckdb` is populated (plan 1), `src/agents/llm_s.py` and `src/agents/llm_f.py` and their signal functions exist (plans 2 and 3), `src/scanner/candidate_scanner.py` exists (plan 4), and `src/optimizer/portfolio.py` exists (plan 5).


"Backtest mode" and "live mode" are defined in Purpose / Big Picture above. The one piece of new machinery this plan needs that no prior plan built is a live membership snapshot for "today" — `plans/01_dataset.md`'s `sp500_membership` table only covers 2020-01-01 through 2024-04-30. This plan's live mode calls the same Wikipedia-scraping logic from `plans/01_dataset.md`'s `src/dataset/membership.py` but for today's date specifically (which, since it walks backward from the current constituent table, is actually the simplest possible case — today's membership is just the current constituent table itself, with zero change-rows to undo).


## Plan of Work


Create `src/flow/interactive.py` with a function `run_pipeline(rebalance_date: date, objective: str, portfolio_value: float, selection: str = "llm_s_only", db_path: str = "data/portfolio.duckdb") -> dict` that: determines whether `rebalance_date` falls within the stored 2020-2024 window or is "today" (or otherwise outside the stored window), and if it is outside the stored window, calls `src/dataset/membership.py`'s reconstruction logic fresh for that date instead of reading `sp500_membership`, and similarly fetches fresh prices/factors/news for that date rather than reading the cached historical tables (reusing the same functions from plans 1 through 3, just pointed at live data instead of the DuckDB cache); depending on `selection`, runs `generate_rule` (plan 2) for the calendar year containing `rebalance_date` and then `screen` (plan 2) for that exact date (skipped entirely when `selection == "llm_f_only"`), and/or runs `screen_month` (plan 3) for that date's month (skipped entirely when `selection == "llm_s_only"`, which is the default per README's Backtest Mode Stage 1 default-selection sentence and per `plans/08_consistency_review.md` finding 9 — skipping means the LLM-F call itself is never made, not merely that its result is discarded, since README frames LLM-F evaluation as "expensive"); runs `scan_with_detail` (plan 4) passing `None` for whichever agent's signals were skipped, so the scanner's own existing `llm_s_only`/`llm_f_only`/`intersection`/`union` branches select correctly; runs `load_returns_matrix` and `compute_weights` (plan 5) with the given `objective` on the resulting candidate list; runs `load_latest_prices` and `allocate_shares` (plan 5) with the given `portfolio_value`; and returns a dict bundling every intermediate result (LLM-S's rule and signals if computed, LLM-F's signals if computed, the scanner's detail dict, the weights, and the allocation) so the CLI layer below can display all of it, not just the final answer — a person steering the process interactively needs to see why a ticker is or is not a candidate, not just the final number of shares.


Create a function `edit_candidates(scan_result: dict, add: list[str], remove: list[str]) -> list[str]` that takes the `candidates` list out of `scan_with_detail`'s output, adds any tickers in `add` not already present, removes any tickers in `remove` that are present, and returns the resulting sorted list — this is deliberately simple set manipulation, matching the fact that by this point in the pipeline both agents have already spoken and the user is just overriding their combined recommendation directly, the same way `plans/04_candidate_scanner.md`'s `scan` function already accepts an arbitrary user-supplied ticker list rather than only the scanner's own output.


Create a CLI entry point `src/flow/cli.py` (a `main()` function, wired up as a `uv run` script or plain `uv run python -m src.flow.cli` invocation — check `pyproject.toml`'s existing `[project.scripts]` conventions, if any exist by the time this plan is implemented, and follow them) that: prompts for a date (or accepts `--date today` / `--date YYYY-MM-DD` as a command-line argument), an objective (GMV/MV/MSR), and a portfolio value; calls `run_pipeline`; prints the candidate list, the branch taken (intersection/union/single-agent), the weights, and the share allocation; then enters a loop offering the user the choice to add tickers, remove tickers, change the objective, or finish — each edit re-runs `compute_weights` and `allocate_shares` (not the two LLM agents again, since the user is overriding the agents' output, not asking them to reconsider) and reprints the updated weights and allocation.


Create `src/flow/backtest.py` with a function `run_full_backtest(objective: str, selection: str = "llm_s_only", db_path: str = "data/portfolio.duckdb") -> pd.DataFrame` that chains `run_pipeline` (using the cached historical data path, not the live path, and passing `selection` through unchanged) across every one of the 52 monthly rebalance dates from `plans/01_dataset.md` in the 2020-2024 window, computing each month's *gross* portfolio return by applying that month's chosen weights to each candidate ticker's `monthly_return` value at the following rebalance date, read directly from `plans/01_dataset.md`'s shared `returns` table (the same table plan 5's `load_returns_matrix` reads for its own, backward-looking estimation purpose — here the read is forward-looking on purpose, since scoring a month's chosen weights needs the return that was realized after the weights were chosen, not before). Convert each month's gross return into a *net* return $r_t$ by subtracting a turnover cost: align month $t$'s weight vector `w_t` and month $t-1$'s weight vector `w_prev` (the zero vector for the first month, per README's $\hat{w}_{0,j}=0$ assumption) onto the union of both months' candidate tickers (missing entries as zero weight), then call `pypfopt.objective_functions.transaction_cost(w_t, w_prev, k=settings.transaction_cost_bps / 10000)` directly as a plain numeric helper (verified against the installed `pypfopt==1.6.0` source to evaluate to a float for `numpy.ndarray` inputs — see `plans/08_consistency_review.md` finding 8's Concrete Steps for the verification) and subtract the result from that month's gross return. Return a DataFrame of these net month-by-month portfolio returns $r_t$, from which the Sharpe ratio is computed as `(r_t.mean() - settings.risk_free_rate / 12) / r_t.std() * (12 ** 0.5)` (a monthly risk-free rate, `settings.risk_free_rate / 12`, subtracted from the mean before annualizing by the square root of 12, matching README's Backtest Mode Stage 2 description of Sharpe Ratio/Returns/Variance being computed from the net returns $r_t$ using a configurable risk-free rate) — this is what produces the number to compare against the paper's own reported 0.6324 S&P 500 baseline Sharpe ratio over the same period, and against the paper's own reported screened-portfolio results, understanding that this project's simplified pipeline (approximate yfinance-derived factors, yfinance news instead of a historical news archive, PyPortfolioOpt's built-in covariance estimators instead of the paper's five custom ones, and a thinner monthly-returns sample than the paper's own estimation window) is not expected to reproduce the paper's exact numbers, only to be in the same broad neighborhood and directionally sensible (screened portfolios beating the unscreened S&P 500 baseline, as the paper claims, is the qualitative result worth checking for; matching the paper's precise 1.1867 headline Sharpe ratio is not). A candidate ticker with no `monthly_return` value at the following rebalance date (delisted mid-month, or otherwise missing) should be excluded from that month's realized gross-return calculation with a logged note, the same drop-and-log pattern used throughout plans 1 and 5, rather than treated as a zero return.


## Concrete Steps


Run every command from the repository root, with all five prior plans already implemented.


Step 1 — check CrewAI's current interactive/human-feedback support before deciding how `src/flow/cli.py`'s editing loop is implemented:

    uv run python -c "import crewai; print(crewai.__version__)"

Then fetch `https://docs.crewai.com/en/concepts/flows` and search for `human_feedback` in the current docs and changelog (`https://docs.crewai.com/en/changelog`). If a stable, documented `@human_feedback` (or equivalent) pattern exists for the installed version, prefer it for the CLI editing loop; if it is missing, experimental, or clearly overkill for what is really just a plain command-line prompt loop, use a plain Python `input()`-based loop calling this plan's own `edit_candidates` function directly — record whichever choice was made, and why, in the Decision Log. This step is already done as of 2026-09-04 against `crewai==1.15.12`: the plain `input()` loop was chosen, and the findings behind that choice are in the Decision Log and Surprises & Discoveries above. Redo it only if CrewAI is upgraded past 1.15.12, in which case re-check whether `@human_feedback`'s `emit` routing still requires an LLM and whether the feedback result is still read as `self.last_human_feedback`.


Step 2 — implement the files in Plan of Work, then run backtest mode for one real historical month end to end:

    uv run python -m src.flow.cli --date 2024-03-01 --objective MSR --value 100000

Expected: printed output showing LLM-S's generated rule and rationale for 2024, LLM-F's per-ticker signals summary, the scanner's chosen branch and candidate list, the MSR weights, and a share allocation totaling close to $100,000 — followed by an interactive prompt to add/remove tickers or finish.


Step 3 — exercise the editing loop at least once in that same run: add one ticker not already in the candidate list and remove one that is, and confirm the reprinted weights and allocation reflect exactly that edited list (spot-check that the removed ticker has zero weight and the added ticker has nonzero weight, assuming it is not immediately excluded by `load_returns_matrix` for insufficient return history).


Step 4 — run live mode:

    uv run python -m src.flow.cli --date today --objective GMV --value 100000

Expected: the same shape of output as Step 2, but built from a freshly-scraped current S&P 500 membership list and freshly-fetched recent prices/news rather than anything in `data/portfolio.duckdb`'s historical tables — confirm this by checking that at least one ticker in the resulting candidate list is a company that was not yet public or not yet S&P-500-eligible in 2020 (for example, a company that IPO'd or was added to the index after 2020), which would be impossible to see in backtest mode and is direct evidence live mode is really using current, not cached, data.


Step 5 — run the full historical backtest and compare against the paper's baseline:

    uv run python -c "
    from src.config.settings import settings
    from src.flow.backtest import run_full_backtest
    returns = run_full_backtest('MSR')
    sharpe = (returns.mean() - settings.risk_free_rate / 12) / returns.std() * (12 ** 0.5)
    print('Realized annualized Sharpe (MSR, net of transaction cost and risk-free rate, this project):', sharpe)
    print('Paper-reported S&P 500 baseline Sharpe, 2020-2024:', 0.6324)
    "

Record the real output number in Outcomes & Retrospective and Surprises & Discoveries, along with a candid assessment of whether it is in a directionally sensible range given the fidelity gaps already recorded elsewhere in this plan set — this step is the closest this project gets to actually validating the paper's central claim, and its result should be reported honestly whether it confirms or fails to confirm that claim.


## Validation and Acceptance


Run `uv run pytest tests/test_interactive_flow.py` and expect all tests to pass. Per `AGENTS.md`'s testing guidance, these tests must not call `yfinance`, any LLM, or hit the live database — they exercise `edit_candidates` (a fixture candidate list, adding and removing known tickers, asserting the exact resulting list) and the Sharpe-ratio arithmetic inside `run_full_backtest`'s calculation (feed it a small fixture Series of monthly returns with a hand-computable mean and standard deviation, and assert the resulting Sharpe ratio matches a value computed by hand).


Acceptance for this plan is: `uv run pytest tests/test_interactive_flow.py` passes; the Concrete Steps Step 2 through Step 5 transcripts (captured for real, not fabricated) show backtest mode, live mode, the editing loop, and the full-window backtest all producing the outputs described above, with Step 5's real Sharpe-ratio comparison recorded honestly in Outcomes & Retrospective.


## Idempotence and Recovery


`run_pipeline` in backtest mode is a pure function of its inputs (aside from the two LLM calls it makes, which are not guaranteed deterministic across repeated calls) and makes no writes to `data/portfolio.duckdb` — safe to call repeatedly. `run_pipeline` in live mode makes fresh network calls every time it runs (by design — that is what makes it "live") and likewise writes nothing to disk, so there is no cached state to go stale or need clearing. `run_full_backtest` is read-only against `data/portfolio.duckdb` and writes nothing; if it is slow to rerun in full during development (52 months, each involving LLM calls for plan 2 and plan 3's screening), consider adding a simple on-disk cache of each month's LLM-S rule and LLM-F signals under `data/` (already gitignored) so repeated backtest runs during development do not re-spend LLM API calls on unchanged historical months — this is an optional development-convenience optimization, not a correctness requirement, and should be recorded in the Decision Log if added.


## Artifacts and Notes


Concrete Steps Step 2's real transcript (2026-09-04), backtest mode only (Step 2's own live-mode/editing-loop expectations are covered once the still-pending items above are implemented): `LLM_S_MODEL=anthropic/claude-opus-4-8 uv run python -m src.flow.cli --date 2024-03-01 --objective MSR --value 100000` (the `LLM_S_MODEL` override needed per the Discovery above). Real `generate_rule(2024)` call against the real 2023-12-01 snapshot produced `buy_condition='(mom12m > 0.55) or (bm > 0.22 and mom12m > -0.7)'`, `sell_condition='(mom12m < -0.85) or (bm < -0.28 and mom12m < 0)'`, with a rationale citing real 2023 mega-cap momentum leaders (META, NVDA, AVGO) and real value/financial-sector clusters (C, BAC, WFC) — structurally consistent with `plans/02_llm_s_agent.md`'s own recorded 2024 rule from its manual-run item, though not numerically identical (expected: LLM-S is not deterministic across calls, per that plan's own Idempotence and Recovery section). `screen` then applied that rule to real 2024-03-01 factor data; the scanner's `llm_s_only` branch produced 112 candidates (`buy_s=112`); `compute_weights` (MSR) and `allocate_shares` produced a 14-ticker discrete allocation (MCK, BLDR, CBOE, CEG, KHC, LLY, MPC, AIZ, VRTX, MSFT, RSG, PHM, CDNS, PANW) totaling $99,937.68 against the requested $100,000, with $62.32 leftover cash — satisfying this plan's "totaling close to $100,000" acceptance bar. Also verified via a separate, cheaper run (`generate_rule`/`screen_month` both mocked) that `selection="llm_s_only"` truly skips the LLM-F call and that an unrecognized `selection` and an out-of-window `rebalance_date` raise `ValueError`/`NotImplementedError` respectively, per the Progress section above.


## Interfaces and Dependencies


This plan depends on every function named in the Interfaces and Dependencies sections of `plans/01_dataset.md` through `plans/05_optimizer_and_allocation.md` — this is the integration plan that calls all of them together, and defines no new lower-level dependency beyond what those five plans already introduced (CrewAI, PyPortfolioOpt, yfinance, DuckDB, pandas). It additionally depends directly on `plans/01_dataset.md`'s `returns` table (read directly by `run_full_backtest`, not through any plan 5 function) as its source of realized next-month returns for backtest scoring — the same table plan 5 reads for its optimizer inputs, per that table's documented dual-consumer role in `plans/01_dataset.md`'s Interfaces and Dependencies section.


At the end of this plan, the following must exist: `src/flow/interactive.py`'s `run_pipeline(rebalance_date: date, objective: str, portfolio_value: float, selection: str = "llm_s_only", db_path: str = "data/portfolio.duckdb") -> dict` and `edit_candidates(scan_result: dict, add: list[str], remove: list[str]) -> list[str]`; `src/flow/cli.py`'s `main()` entry point; and `src/flow/backtest.py`'s `run_full_backtest(objective: str, selection: str = "llm_s_only", db_path: str = "data/portfolio.duckdb") -> pd.DataFrame`. This is the last plan in the sequence described in `README.md` — nothing in this repository's plan set depends on this plan, so there is no further Interfaces section to satisfy beyond what a human user directly invoking `src/flow/cli.py` needs. This plan additionally now depends on `src/config/settings.py`'s `transaction_cost_bps` and `risk_free_rate` fields (added while resolving `plans/08_consistency_review.md` findings 8 and 10) and on `pypfopt.objective_functions.transaction_cost`, used as a plain numeric helper rather than inside an `EfficientFrontier.add_objective()` call.
